defaults:
  - _self_
  - model: hyperencoder_basic
  - data: hyperencoder
  - training: default

# Main application configuration
# Task selection - can be overridden via command line: task=train or task=pre_encode
task: train

# Additional settings can be added here
project_name: "audio-hyperencoder"
description: "Semantic audio representation learning with nested autoencoders" 