{
  "$comment": "Generated from hyperencoder.datamodels.model_config.DemoConfig",
  "$id": "https://hyperencoder.ai/schemas/demo_config.schema.json",
  "additionalProperties": false,
  "description": "Configuration for demo/evaluation settings.",
  "properties": {
    "demo_every": {
      "default": 20,
      "description": "Generate demo every N steps",
      "minimum": 1,
      "title": "Demo Every",
      "type": "integer"
    },
    "demo_length": {
      "anyOf": [
        {
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Length of demo sequences",
      "title": "Demo Length"
    },
    "max_demos": {
      "default": 10,
      "description": "Maximum number of demos to generate",
      "minimum": 1,
      "title": "Max Demos",
      "type": "integer"
    },
    "save_demos": {
      "default": true,
      "description": "Whether to save demo outputs",
      "title": "Save Demos",
      "type": "boolean"
    }
  },
  "title": "DemoConfig",
  "type": "object"
}