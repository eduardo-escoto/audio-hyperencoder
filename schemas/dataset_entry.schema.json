{
  "$comment": "Generated from hyperencoder.datamodels.data_config.DatasetEntry",
  "$id": "https://hyperencoder.ai/schemas/dataset_entry.schema.json",
  "additionalProperties": false,
  "description": "Configuration for a single dataset entry.",
  "properties": {
    "name": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional name for the dataset",
      "title": "Name"
    },
    "path": {
      "description": "Path to the dataset directory or file",
      "examples": [
        "/path/to/dataset",
        "./data/pre_encoded",
        "${oc.env:DATA_ROOT}/pre_encoded_babyslakh"
      ],
      "title": "Path",
      "type": "string"
    },
    "weight": {
      "default": 1.0,
      "description": "Weight for this dataset when combining multiple datasets",
      "minimum": 0.0,
      "title": "Weight",
      "type": "number"
    }
  },
  "required": [
    "path"
  ],
  "title": "DatasetEntry",
  "type": "object"
}