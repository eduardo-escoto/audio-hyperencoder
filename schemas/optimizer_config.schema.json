{
  "$comment": "Generated from hyperencoder.datamodels.model_config.OptimizerConfig",
  "$id": "https://hyperencoder.ai/schemas/optimizer_config.schema.json",
  "additionalProperties": false,
  "description": "Configuration for optimizer settings.",
  "properties": {
    "_target_": {
      "default": "torch.optim.AdamW",
      "description": "Target optimizer class",
      "examples": [
        "torch.optim.AdamW",
        "torch.optim.Adam",
        "torch.optim.SGD"
      ],
      "title": "Target",
      "type": "string"
    },
    "betas": {
      "default": [
        0.9,
        0.999
      ],
      "description": "Beta parameters for Adam-based optimizers",
      "items": {
        "type": "number"
      },
      "title": "Betas",
      "type": "array"
    },
    "eps": {
      "default": 1e-08,
      "description": "Epsilon for numerical stability",
      "minimum": 1e-12,
      "title": "Eps",
      "type": "number"
    },
    "lr": {
      "default": 0.0001,
      "description": "Learning rate",
      "maximum": 1.0,
      "minimum": 1e-08,
      "title": "Lr",
      "type": "number"
    },
    "weight_decay": {
      "default": 0.001,
      "description": "Weight decay (L2 regularization)",
      "minimum": 0.0,
      "title": "Weight Decay",
      "type": "number"
    }
  },
  "title": "OptimizerConfig",
  "type": "object"
}