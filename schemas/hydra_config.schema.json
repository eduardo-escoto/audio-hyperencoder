{
  "$comment": "Generated from hyperencoder.datamodels.hydra_config.HydraConfig",
  "$defs": {
    "HydraJobConfig": {
      "additionalProperties": false,
      "description": "Configuration for Hydra job settings.",
      "properties": {
        "name": {
          "default": "hyperencoder_${hydra:runtime.choices.model}",
          "description": "Job name template",
          "title": "Name",
          "type": "string"
        }
      },
      "title": "HydraJobConfig",
      "type": "object"
    },
    "HydraRunConfig": {
      "additionalProperties": false,
      "description": "Configuration for Hydra run settings.",
      "properties": {
        "dir": {
          "default": "outputs/${now:%Y-%m-%d_%H-%M-%S}",
          "description": "Output directory for Hydra runs",
          "title": "Dir",
          "type": "string"
        }
      },
      "title": "HydraRunConfig",
      "type": "object"
    },
    "HydraSweepConfig": {
      "additionalProperties": false,
      "description": "Configuration for Hydra sweep settings.",
      "properties": {
        "dir": {
          "default": "multirun/${now:%Y-%m-%d_%H-%M-%S}",
          "description": "Output directory for multirun sweeps",
          "title": "Dir",
          "type": "string"
        },
        "subdir": {
          "default": "${hydra:job.num}",
          "description": "Subdirectory template for sweep runs",
          "title": "Subdir",
          "type": "string"
        }
      },
      "title": "HydraSweepConfig",
      "type": "object"
    }
  },
  "$id": "https://hyperencoder.ai/schemas/hydra_config.schema.json",
  "additionalProperties": false,
  "description": "Configuration for Hydra framework settings (hydra/default.yaml).",
  "properties": {
    "defaults": {
      "default": [
        "_self_"
      ],
      "description": "Default Hydra configurations to include",
      "items": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object"
          }
        ]
      },
      "title": "Defaults",
      "type": "array"
    },
    "job": {
      "$ref": "#/$defs/HydraJobConfig",
      "description": "Job configuration settings"
    },
    "run": {
      "$ref": "#/$defs/HydraRunConfig",
      "description": "Run configuration settings"
    },
    "sweep": {
      "$ref": "#/$defs/HydraSweepConfig",
      "description": "Sweep configuration settings"
    }
  },
  "title": "HydraConfig",
  "type": "object"
}